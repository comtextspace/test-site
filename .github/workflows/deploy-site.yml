name: Deploy Site to GitHub Pages

on:
  - push
  - workflow_dispatch

jobs:
  call-deploy:
    uses: comtextspace/comtext-builder-workflow/.github/workflows/deploy.yml@main
    with:
      url: null
# Для добавления домена уберите комментарии и укажите свой домен
